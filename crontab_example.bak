# Edit this file to introduce tasks to be run by cron.
# 
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
# 
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').
# 
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
# 
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
# 
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
# 
# m h  dom mon dow   command

PROJ_ROOT=/home/pi/autocap/
LOG_DIR=$PROJ_ROOT/webroot/logs/

# run the indexer script every 10min to make sure its up to date
* */10 * * * /usr/bin/python3 $PROJ_ROOT/indexer.py

# fetch and log the health status of the rpi once per hour
*/1 * * * * /usr/bin/python3 $PROJ_ROOT/health_monitor.py --output $LOG_DIR/health_log.txt

# dump current crontab config into a log file once per hour
*/1 * * * * crontab -l > $LOG_DIR/crontab_config.txt

# Run sunrise scripts at 3:00 AM daily, always before sunrise here
0 3 * * * /usr/bin/python3 $PROJ_ROOT/scheduler_sun.py --cam cam2 --mode sunrise --start_offset -45 --end_offset 45 --interval 10 >> $LOG_DIR/logs_sunrise_cam2.txt 2>&1

# Run sunset scripts at 3:00 PM daily, always before sunset here.
0 15 * * * /usr/bin/python3 $PROJ_ROOT/scheduler_sun.py --cam cam1 --mode sunset --start_offset -45 --end_offset 45 --interval 10 >> $LOG_DIR/logs_sunset_cam1.txt 2>&1

# fixed schedule to record at the same time every day
0 9 * * * /usr/bin/python3 $PROJ_ROOT/record.py --cam cam2 --duration 1 --interval 1 --skip-video --suffix "_9am" >> $LOG_DIR/logs_9a_cam2.txt 2>&1
0 17 * * * /usr/bin/python3 $PROJ_ROOT/record.py --cam cam1 --duration 1 --interval 1 --skip-video --suffix "_5pm" >> $LOG_DIR/logs_5p_cam1.txt 2>&1

# --- CLEANUP ---
# Delete old videos every night at 1AM
#0 1 * * * find /var/www/html -name "*.mp4" -type f -mtime +14 -delete
